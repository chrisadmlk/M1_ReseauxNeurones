package Tools_Classes;

public class ChartMaker {


	/* variables à ajouter : une liste de dataset correspondant aux points et aux droites données en sortie du réseaux de neurones
	 * 
	 * 
	 * méthodes: 	add / retrieve series (on passe un objet DataToUse en paramètre, qui encapsule un tableau de points)
	 *				add / retrieve line  (on passe les 3 variables constitutives en paramètre)
	 *				get chart
	 *
	*/
	
	
	
	
	
	/* fonctionnement de jfreechart :
	 * 1) on crée un objet de type dataset
	 * 2) on lui ajout une series (= les data)   (possibilité d'ajouter plusieurs séries)
	 * 3) on créé un objet JFreeChart grâce au ChartFactory auquel on passe le dataset en paramètre
	 * 
	 */
	
	
}
